# Local Jarvis Default Companion Assets

This pack is a placeholder-only companion pack for the visible desktop shell.

The desktop app currently renders the companion with Qt painting primitives and
uses the text files in `placeholders/` as asset slots for future sprite/image
work. Do not add real audio, capture, OCR, ASR, or cloud behavior here.
